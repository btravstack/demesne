---
---

Test and docs only — no user-facing change. Closes audit findings F1–F6: adds
tests for the repeated-`collect`-member contribution (#13) and the
introspection no-build safety property (#15), and fixes doc inconsistencies
(`inject` in the exact-edges list, `fromSafePromise` in the re-export list,
the `roadmap.md` → `resources-and-scopes.md` rename, and two comparison-table
corrections).
